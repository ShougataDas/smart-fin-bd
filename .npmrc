# Disable React Native CLI environment setup
RN_NO_SETUP_ENV=1

# Skip platform-specific setup
SKIP_SETUP_ENV=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable audit warnings for faster installs
audit=false